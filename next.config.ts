import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: false, // Temporarily disable StrictMode for testing react-beautiful-dnd
  /* config options here */
};

export default nextConfig;
